defmodule Mobupay.AccountFixtures do
  @moduledoc """
  This module defines test helpers for creating
  entities via the `Mobupay.Account` context.
  """

  @doc """
  Generate a user.
  """
  # def user_fixture(attrs \\ %{}) do
  #   {:ok, user} =
  #     attrs
  #     |> Enum.into(%{
  #       country_code: "some country_code",
  #       msisdn: "some msisdn",
  #       reference: "some reference"
  #     })
  #     |> Mobupay.Account.create_user()

  #   user
  # end
end
